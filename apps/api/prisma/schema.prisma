generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Domain modellerini (Account, Transaction, Invoice, vb.)
// birazdan buraya ekleyeceğiz. Şimdilik bağlantı ve migration pipeline'ı için boş bırakıyoruz.

